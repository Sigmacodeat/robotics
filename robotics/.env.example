# Beispiel-Umgebungsvariablen für Businessplan-Site
# Kopiere diese Datei zu `.env.local` und passe die Werte an.

# Öffentlich verfügbare Basis-URL deiner Seite (ohne Slash am Ende)
# Lokal (laut Projekt-Setup läuft der Dev-Server auf Port 3001)
NEXT_PUBLIC_SITE_URL=http://localhost:3001
