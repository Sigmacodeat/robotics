const problem = {
  title: "Problem",
  overviewTitle: "Problem Overview",
  // Optional: defaults; component can still receive props
  defaults: {
    empty: "No problems defined",
  },
} as const;

export default problem;
