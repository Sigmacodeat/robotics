// Extracted from old messages; standalone content structure
const exit = {
  // Add specific exit details here if available. Old EN file proxied to full bp.content.exit.
} as const;

export default exit;
